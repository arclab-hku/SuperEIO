%YAML:1.0
---

#common parameters
imu_topic: "/imu/data"
image_topic: "/davis346/image_raw"
event_topic: "/davis/left/events"
output_path: "/home/cpy/Datasets/output"

#camera calibration 
model_type: PINHOLE
camera_name: camera
image_width: 640
image_height: 480
distortion_parameters:
   k1: -0.031982
   k2: 0.041966
   p1: -0.000507
   p2: -0.001031
projection_parameters:
   fx: 327.32749
   fy: 327.46184
   cx: 304.97749
   cy: 235.37621


# Extrinsic parameter between IMU and Camera.
estimate_extrinsic: 1   # 0  Have an accurate extrinsic parameters. We will trust the following imu^R_cam, imu^T_cam, don't change it.
                        # 1  Have an initial guess about extrinsic parameters. We will optimize around your initial guess.
                        # 2  Don't know anything about extrinsic parameters. You don't need to give R,T. We will try to calibrate it. Do some rotation movement at beginning.                        
#If you choose 0 or 1, you should write down the following matrix.
#Rotation from camera frame to imu frame, imu^R_cam
extrinsicRotation: !!opencv-matrix
   rows: 3
   cols: 3
   dt: d
   data: [0.02755145, -0.99942203, 0.01991329,
          0.02426745, -0.01924626, -0.99952022,
          0.99932578, 0.02802148, 0.02372317]

#Translation from camera frame to imu frame, imu^T_cam
extrinsicTranslation: !!opencv-matrix
   rows: 3
   cols: 1
   dt: d
   data: [0.07046234 , 0.02418805, -0.12929087]

T_camera_imu: 1  # if we set the R and T as IMU to camera, we need to give an T to them
# T_camera_imu: 0  # if we set the R and T as c to I

#time surface map
ignore_polarity: 0 #true 1 false 0;
decay_ms: 20 # 20
median_blur_kernel_size: 0 #1 or 0
feature_filter_threshold: 0.05


#feature traker paprameters
TS_LK_threshold: 128.0   # the feature detection is based on the time surface
max_cnt: 200            # max feature number in feature tracking
min_dist: 5            # min distance between two features 
freq: 15 #10                # frequence (Hz) of publish tracking result. At least 10Hz for good estimation. If set 0, the frequence will be same as raw image 
F_threshold: 1.0        # ransac threshold (pixel)
show_track: 1           # publish tracking image as topic
flow_back: 1            # perform forward and backward optical flow to improve feature tracking accuracy
equalize: 0             # if image is too dark or light, trun on equalize to find enough features
fisheye: 0              # if using fisheye, trun on it. A circle mask will be loaded to remove edge noisy points
Num_of_thread: 2        #number of the threads for the big event data process

#optimization parameters
max_solver_time: 0.04  # max solver itration time (ms), to guarantee real time
max_num_iterations: 8   # max solver itrations, to guarantee real time
keyframe_parallax: 10.0 # keyframe selection threshold (pixel)

#imu parameters       The more accurate parameters you provide, the better performance
acc_n: 0.2 #0.012655720309610252 #0.2 #4.2153479580575477e-02 #0.1          # accelerometer measurement noise standard deviation. #0.2
gyr_n: 0.05 #0.007294729852113113 #0.05 #3.4869979619824697e-03 #0.01         # gyroscope measurement noise standard deviation.     #0.05 
acc_w: 0.002 #5.6386016813618435e-04 #0.002 #8.5654389798364998e-04 #0.0002          # accelerometer bias random work noise standard deviation.  #0.02
gyr_w: 4.0e-5 #6.996094830870257e-05 ##2.0e-5      # gyroscope bias random work noise standard deviation.     #4.0e-5
g_norm: 9.805 #9.80766         # gravity magnitude

#loop closure parameters
loop_closure: 1                    # start loop closure
loop_closure_topic: "/event_detector/timesurface_map"   #use the time surface for loop closure
load_previous_pose_graph: 0        # load and reuse previous pose graph; load from 'pose_graph_save_path'
fast_relocalization: 0             # useful in real-time and large project
pose_graph_save_path: "/home/cpy/catkin_ws_dvs/src/EVIO/pose_graph/output/pose_graph/" # save and load path

#unsynchronization parameters
estimate_td: 0                      # online estimate time offset between camera and imu
td: 0.0                             # initial value of time offset. unit: s. readed image clock + td = real image clock (IMU clock)

#rolling shutter parameters
rolling_shutter: 0                  # 0: global shutter camera, 1: rolling shutter camera
rolling_shutter_tr: 0               # unit: s. rolling shutter read out time per frame (from data sheet). 

#visualization parameters
save_image: 1                   #  (DEBUG_IMAGE)   save image in pose graph for visualization prupose; you can close this function by setting 0 
visualize_imu_forward: 1        # output imu forward propogation to achieve low latency and high frequence results
visualize_camera_size: 0.4      # size of camera marker in RVIZ
save_loop_match: 1

super_eventpoint:
  max_keypoints: 400 #200
  keypoint_threshold: 0.015 #0.004, 0.015
  remove_borders: 4 
  input_tensor_names: 
    - "input"
  output_tensor_names:
    - "scores"
    - "descriptors"
  onnx_file: "super_eventpoint.onnx"
  engine_file: "super_eventpoint.engine"
  dla_core: -1

super_eventmatch:
  image_width: 320 # 346
  image_height: 240 # 260
  input_tensor_names:
    - "keypoints_0"
    - "scores_0"
    - "descriptors_0"
    - "keypoints_1"
    - "scores_1"
    - "descriptors_1"
  output_tensor_names:
    - "scores"
  onnx_file: "super_eventmatch.onnx"
  engine_file: "super_eventmatch.engine"
  dla_core: -1
